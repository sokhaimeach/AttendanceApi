# Attendance_Api

A RESTful API for managing attendance records, students, classes and authentication.

## Features
- User authentication (JWT)
- Student and class management
- Attendance recording and reporting
- Role-based access (admin/teacher)

## Prerequisites
- Runtime: {RUNTIME}
- Install dependencies: `{INSTALL_COMMAND}`

## Environment
Required environment variables:
- PORT={PORT}
- DATABASE_URL={DB_CONNECTION_STRING}
- JWT_SECRET={JWT_SECRET}

## Setup
1. Clone repository
2. Install deps: `{INSTALL_COMMAND}`
3. Create a .env file with the environment variables above

## Run
Start the server: